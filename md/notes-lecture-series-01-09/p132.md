## Continuous Random Variables

**Definition**
The **cumulative distribution function** $F(x)$ for a continuous rv $X$ is defined for every number $x$ by

$$ F(x) = P(X \le x) = \int_{-\infty}^{x} f(y) dy $$

For each $x$, $F(x)$ is the area under the density curve to the left of $x$. This is illustrated in Figure, where $F(x)$ increases smoothly as $x$ increases.

Figure: Two side-by-side graphs illustrating a pdf and its associated cdf. The left graph shows a bell-shaped density curve $f(x)$ with the area under the curve to the left of $x=8$ shaded. An arrow points to this shaded region and is labeled $F(8)$. The x-axis has numeric ticks at 5, 8, and 10. The right graph shows the cumulative distribution function $F(x)$ as a smooth S-shaped curve. The x-axis has numeric ticks at 5, 8, and 10. The y-axis has numeric ticks at .5 and 1. A dashed vertical line rises from $x=8$ on the x-axis to the curve, and a dashed horizontal line leads from that point to the y-axis, where it is labeled $F(8)$.

A pdf and associated cdf
MUJ | DR VIVEK SINGH

page:132
