## Continuous Random Variables

Figure illustrates the second part of this proposition; the desired probability is the shaded area under the density curve between $a$ and $b$, and it equals the difference between the two shaded cumulative areas.

Figure: Three plots of a probability density function $f(x)$ versus $x$. The first plot shows the area shaded between $x = a$ and $x = b$ under a unimodal curve. An equals sign ($=$) follows, then a second plot showing the area shaded from the left tail up to $x = b$. A minus sign ($-$) follows, then a third plot showing the area shaded from the left tail up to $x = a$. Caption: "Computing $P(a \le X \le b)$ from cumulative probabilities"

This is different from what is appropriate for a discrete integer valued random variable:
$P(a \le X \le b) = F(b) - F(a - 1)$ when $a$ and $b$ are integers.

MUJ | DR VIVEK SINGH
page:135
